import {View} from 'react-native';
import styled from 'styled-components/native';

export const StyledInfoCard = styled(View)`
  width: 100%;
  height: 200;
  bottom: 0;
  background-color: white;
  position: absolute;
`;
